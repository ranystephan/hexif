"""HEXIF Spatial Cytometry Workbench package.

Launch::

    pip install -e ".[web]"
    hexif serve-workbench --bundle /path/to/validated_bundle

or directly::

    python -m webapp.workbench_app --bundle /path/to/validated_bundle
"""
